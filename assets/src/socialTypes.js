/**
 * Types matching Motoko Associates / social graph.
 * (JSDoc for editor tooling — project is React without full TS build.)
 *
 * @typedef {import("@dfinity/principal").Principal} Principal
 *
 * @typedef {Object} Associates
 * @property {Principal[]} following
 * @property {Principal[]} followers
 */

export {};
