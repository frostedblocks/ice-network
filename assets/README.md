# I.C.E. Frontend

React + Vite UI for the I.C.E. Internet Computer app.

```bash
# From project root (after dfx deploy)
cd frontend
npm install
npm run dev
```

Canister IDs are injected via `vite.config.js` from `../.dfx/.../canister_ids.json` and `frontend/.env.local`.
